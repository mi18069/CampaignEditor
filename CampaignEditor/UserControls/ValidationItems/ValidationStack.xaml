<UserControl x:Class="CampaignEditor.UserControls.ValidationItems.ValidationStack"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CampaignEditor.UserControls.ValidationItems"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800" Background="White">
    <Grid>
        <!-- <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
         <Button x:Name="btnShowHideAll" Grid.Row="0" Content="Show All" HorizontalAlignment="Stretch" 
                Background="LightGoldenrodYellow" Click="btnShowHideAll_Click"
                Height="30"/> 
        <ScrollViewer x:Name="svGrid" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="spValidationDays"/>
        </ScrollViewer> -->
        <ScrollViewer x:Name="svGrid" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="spValidationDays"/>
        </ScrollViewer>
    </Grid>
</UserControl>
