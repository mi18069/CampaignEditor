<UserControl x:Class="CampaignEditor.UserControls.ClientCmpsTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CampaignEditor.UserControls"
             xmlns:converters="clr-namespace:CampaignEditor.Converters"
             xmlns:entities="clr-namespace:Database.Entities;assembly=Database"
             xmlns:cmpDTO="clr-namespace:Database.DTOs.CampaignDTO;assembly=Database"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400" d:Background="White">
    <UserControl.Resources>
        <converters:DateComparisonConverter x:Key="DateComparisonConverter"/>
        <converters:UserLevelToBooleanConverter x:Key="UserLevelToBooleanConverter"/>
        <converters:TvBoolToImageConverter x:Key="TvBoolToImageConverter"/>
    </UserControl.Resources>

    <Grid>
        <TreeView Name="_tvUsers" MouseDoubleClick="_tvUsers_MouseDoubleClick">
            <TreeView.Resources>
                <!-- ContextMenu for clicked on empty space -->
                <ContextMenu x:Key="EmptyContextMenu" >
                    <ContextMenu.DataContext>
                        <local:TreeViewData />
                    </ContextMenu.DataContext>
                    <MenuItem Header="New client" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=0}" Click="NewClientMenuItem_Click"/>
                </ContextMenu>

                <!-- ContextMenu for Clients -->
                <ContextMenu x:Key="ClientContextMenu" >
                    <ContextMenu.DataContext>
                        <local:TreeViewData />
                    </ContextMenu.DataContext>
                    <MenuItem Header="Users of client" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=2}" Click="UsersOfClientMenuItem_Click"/>
                    <MenuItem Header="New campaign" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=1}" Click="NewCampaignMenuItem_Click"/>
                    <MenuItem Header="Rename client" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=0}" Click="RenameClientMenuItem_Click"/>
                    <MenuItem Header="New client" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=0}" Click="NewClientMenuItem_Click"/>
                    <MenuItem Header="Delete client" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=0}" Click="DeleteClientMenuItem_Click"/>
                </ContextMenu>

                <!-- ContextMenu for Campaigns -->
                <ContextMenu x:Key="CampaignContextMenu">
                    <ContextMenu.DataContext>
                        <local:TreeViewData />
                    </ContextMenu.DataContext>
                    <MenuItem Header="Edit campaign" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=1}" Click="EditCampaignMenuItem_Click"/>
                    <MenuItem Header="Rename campaign" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=1}" Click="RenameCampaignMenuItem_Click"/>
                    <MenuItem Header="Delete campaign" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=1}" Click="DeleteCampaignMenuItem_Click"/>
                    <MenuItem Header="Duplicate campaign" IsEnabled="{Binding UserLevel, Converter={StaticResource UserLevelToBooleanConverter}, ConverterParameter=1}" Click="DuplicateCampaignMenuItem_Click"/>
                </ContextMenu>

            </TreeView.Resources>
            <TreeView.ContextMenu>
                <!-- Set one of the context menus as the default -->
                <StaticResource ResourceKey="EmptyContextMenu"/>
            </TreeView.ContextMenu>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type entities:ClientCampaignsList}" ItemsSource="{Binding Campaigns}">
                    <TextBlock Text="{Binding Client.clname}" ContextMenu="{StaticResource ClientContextMenu}"/>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate DataType="{x:Type cmpDTO:CampaignDTO}">
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Height="20" Source="{Binding tv, Converter={StaticResource TvBoolToImageConverter}}" />
                                <TextBlock Text="{Binding cmpname}" ContextMenu="{StaticResource CampaignContextMenu}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding cmpsdate, Converter={StaticResource DateComparisonConverter}}" Value="True">
                                                    <Setter Property="Foreground" Value="DarkGreen"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding cmpsdate, Converter={StaticResource DateComparisonConverter}}" Value="False">
                                                    <Setter Property="Foreground" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding cmpedate, Converter={StaticResource DateComparisonConverter}}" Value="True">
                                                    <Setter Property="Foreground" Value="DarkRed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>